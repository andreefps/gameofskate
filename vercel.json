{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd apps/native && npx expo export -p web",
  "outputDirectory": "apps/native/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
